<nav class="navbar navbar-light" style="background-color: #D3D3D3;">
  <%= link_to "TestGuru", root_path, class: 'navbar-brand' %>

  <span class="navbar-text-center">
    <% if user_signed_in? %>
      <%= link_to t('links.admin'), admin_tests_path, class: 'navbar-brand' if current_user.admin?%>
    
      Welcome, <b><%= current_user.email %></b> Guru | 
        <%= link_to 'Log out', destroy_user_session_path,
                              method: :delete,
                              data: { confirm: t("links.confirmation") } %>
    <% else %>
        <%= link_to 'Login', new_user_session_path %> | 
        <%= link_to 'Signup', new_user_registration_path %>
    <% end %>
  </span>
</nav>
