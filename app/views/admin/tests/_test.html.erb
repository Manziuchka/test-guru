    <tr>
      <td><%= test.id %></td>
      <td><%= test.title %></td>
      <td><%= test.level %></td>
      <td><%= test.questions.count%></td>
      <td><%= link_to t("links.show"), admin_test_path(test) %></td>
	    <td><%= link_to t("links.update"), edit_admin_test_path(test) %></td>
      <td><%= link_to t("links.delete"), admin_test_path(test), 
              method: :delete,
              data: { confirm: t("links.confirmation") } %></td>
    </tr>